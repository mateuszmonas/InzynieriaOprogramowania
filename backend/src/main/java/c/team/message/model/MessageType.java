package c.team.message.model;

public enum MessageType {

    COMMENT,
    REPLY,
    CONNECT,
    DISCONNECT,
    GUEST_APPROVAL,
    QUIZ,
    QUIZ_ANSWERS,
    EMOTE
    // QUIZ, different types of it
}
